import { cartoonImageHeight } from 'bonsai/helpers/cartoon-image-height';
import { module, test } from 'qunit';

module('Unit | Helper | cartoon image height');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cartoonImageHeight([42]);
  assert.ok(result);
});
