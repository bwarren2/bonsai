import { getCartoonHelpTitle } from 'bonsai/helpers/get-cartoon-help-title';
import { module, test } from 'qunit';

module('Unit | Helper | get cartoon help title');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getCartoonHelpTitle([42]);
  assert.ok(result);
});
