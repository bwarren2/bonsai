import { cartoonSelect } from 'bonsai/helpers/cartoon-select';
import { module, test } from 'qunit';

module('Unit | Helper | cartoon select');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cartoonSelect([42]);
  assert.ok(result);
});
