import { getCartoonHelp } from 'bonsai/helpers/get-cartoon-help';
import { module, test } from 'qunit';

module('Unit | Helper | get cartoon help');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getCartoonHelp([42]);
  assert.ok(result);
});
