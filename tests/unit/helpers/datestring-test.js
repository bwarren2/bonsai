import { datestring } from 'bonsai/helpers/datestring';
import { module, test } from 'qunit';

module('Unit | Helper | datestring');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = datestring([42]);
  assert.ok(result);
});
