<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>

      {{#link-to 'index' class='navbar-brand'}}
        <img src="/images/bonsai.png" alt="Bonsai" />
      {{/link-to}}
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        {{#active-link}}
          {{#link-to 'brainstorm'}}braindump{{/link-to}}
        {{/active-link}}
        {{#active-link}}
          {{#link-to 'refine'}}refine{{/link-to}}
        {{/active-link}}
        {{#active-link}}
          {{#link-to 'plan'}}plan{{/link-to}}
        {{/active-link}}
        {{#active-link}}
          {{#link-to 'execute'}}execute{{/link-to}}
        {{/active-link}}
        {{#active-link}}
          {{#link-to 'review'}}review{{/link-to}}
        {{/active-link}}
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

<div class="container">
  {{outlet}}
</div>

<footer class="footer">
  <div class="container">
    <p class="text-muted">
    &copy; 2016 Bonsai Tasks. Logo from the Noun Project, &copy; by Liv Iko.
    </p>
  </div>
</footer>
